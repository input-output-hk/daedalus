Planner: Iteration 1
Timestamp: 2026-09-15T12:00:00Z

Drafted from the task's entry in the graph after `task-020`.

Critique of Iteration 1:

1. *The first draft rendered the sentence from `task-020`'s verdict.* They are
   different questions. The verdict is about two numbers disagreeing and is
   suppressed for a published zero with no setting; this sentence is about one
   number being uncheckable and is true then too. Rewritten as its own condition,
   with a case that fails under the other reading.

2. *The sentence did not name the number.* "Could not be verified" about a figure
   the user has to go and find is weaker than one that names it, and the number
   is the thing they are being asked to decide about.

3. *Nothing covered a user who has already set a value.* The sentence is about
   what the issuer published, so it holds whether or not the user has chosen
   something. Added.

4. *The container was drafted as part of the sentence's own element.*
   `task-029` adds a control to the same place immediately after this, and the
   task graph asks for an order that does not leave two separate additions to one
   layout. The container is created here and holds one thing; `task-029` puts its
   control in it.

Changes made in response: the independent condition, the number in the sentence,
the user-setting case, and the container.

Scope guard: nothing on the token row, no icon, not the refresh control.

Outcome: approved
