Implementation: Iteration 1
Timestamp: 2026-09-14T23:34:50Z

Changes made:
- Removed `storybook/stories/staking/Legacy.stories.tsx` and the 17 tracked files under
  `source/renderer/app/components/staking/legacy/`: eight components, eight stylesheets and the
  `_stakingConfig.scss` partial belonging to that directory.
- Regenerated the four tracked i18n artifacts that carried the six messages
  `StakingChartTooltip.tsx` defined.
- Recorded three corrections in the `task-003` entry: the i18n consequence and why it matters, the
  `.scss.d.ts` note being wrong because `.gitignore:141` ignores them, and the refinement to locked
  decision 5's wording about the five unreferenced components.
- Set `task-003.status` to `completed` and `## Build Status` to `completed`.

Files touched:
- 18 removals
- `translations/messages.json`
- `source/renderer/app/i18n/locales/defaultMessages.json`
- `source/renderer/app/i18n/locales/en-US.json`
- `source/renderer/app/i18n/locales/ja-JP.json`
- `.agent/plans/storybook-modernization/storybook-modernization-tasks.json`
- `.agent/plans/storybook-modernization/task-plans/task-003.md`
- `.agent/plans/storybook-modernization/task-plans/task-003-plan-review.md`
- `.agent/plans/storybook-modernization/task-plans/task-003-impl-review.md`

Verification run:
- `git status --short` before any check: 18 `D` entries, four `M` entries on the i18n artifacts, and
  nothing untracked outside `.agent/`.
- `source/renderer/app/components/staking/legacy/` is absent from disk. The eight gitignored
  `.scss.d.ts` files were cleared with it, so no stale directory remains. They were untracked and
  the commit is unaffected either way.
- `source/renderer/app/components/staking/_stakingConfig.scss` is present and unmodified. Only the
  `legacy/` copy went, which was the risk worth guarding: two files of the same name in one subtree,
  one with five surviving importers.
- i18n regenerated by the proved route: `nix build .#internal.x86_64-linux.node_modules`, then
  `formatjs extract` and `translations/translation-runner.ts` from that tree. The diff is 82 lines
  across four files, every one a deletion and not one an insertion. `defaultMessages.json` and
  `translations/messages.json` each lost the single `StakingChartTooltip.tsx` descriptor block, and
  `en-US.json` and `ja-JP.json` each lost six keys.
- A grep for `staking.chart.tooltip` and for `staking/legacy` across `source/`, `storybook/`,
  `tests/` and `translations/` returns nothing.
- Per-component grep across `source/`, `storybook/`, `tests/`, `features/` and `translations/`
  returns nothing for `BlockGenerationInfo`, `StakingChartTooltipItem`, `StakingChartTooltip`,
  `StakingChart`, `StakingSwitch`, `StakingSystemStateElement` and `StakingSystemState`. Second
  acceptance criterion met.
- Sidebar regenerated and diffed against the post-`task-002` capture. The only difference is the
  whole root-level `StakingChart` group disappearing with its two registrations. Counts go from 15
  groups, 50 titles, 261 registrations and `UNREACHABLE 2` to 14, 49, 259 and `UNREACHABLE 0`.
- `nix build --no-link .#checks.x86_64-linux.storybook` exit 0, `.compile` exit 0 on derivation
  `yzxicnczzmp24f709i7rl6az258ns21f-daedalus-compile.drv`, `.i18n` exit 0, and `.lint` exit 0 at
  5445 warnings against 5468 after `task-002`. First acceptance criterion met, and `i18n` with it.

One prediction was slightly wrong and is corrected here rather than left: the plan said the distinct
title count would go from 50 to 49 and did not say the group count would move. `StakingChart` was a
root-level title with no separator, so it was its own top-level group as well as its own panel, and
the group count went from 15 to 14. The registration arithmetic was right; the tree arithmetic was
one line short.

Deviations from the approved plan:
- None in the change. The removals were performed by the operator against the list this task
  produced.

User interaction is now required:
- No.

Outcome: Both acceptance criteria met, the i18n regeneration is pure deletion of the six removed
ids, and four checks are green; ready for code review

Code Review: Iteration 1
Timestamp: 2026-09-14T23:39:20Z

Summary:
- Approved. Eighteen files removed on a negative that was established two independent ways, four
  generated artifacts regenerated rather than hand-edited, and the required check that the task
  entry never mentioned is green.

Blocking findings:
- None.

Non-blocking observations:
- The i18n finding is the substance of this task's planning. The entry listed two acceptance
  criteria, neither of which would have failed if the artifacts had been left stale; the check that
  would have failed is one the entry does not name. That is the second time in this phase a task
  entry has been short of what the task actually requires, after `task-001`'s grep figure.
- Proving the regeneration round trip on the unchanged tree first is what makes the 82-line diff
  believable. A generator run on a changed tree produces a diff either way, and the only way to know
  it contains nothing but the intended change is to have watched it produce nothing at all.
- Two files named `_stakingConfig.scss` in one subtree, one deleted and one kept, was the sharpest
  edge here. It was found in planning, guarded in the list, and confirmed after.
- The dead-subtree correction to locked decision 5 is worth having in the entry. Decision 5's
  wording would have a reviewer expect seven of the eight greps to come back empty before the
  deletion; four of them do not, and the reason is benign.
- `UNREACHABLE` reaching 0 is a small piece of leverage for the rest of the plan. The extractor's
  reachability column now reads clean, so `task-010`'s glob has an unambiguous before-state.
- The plan's tree arithmetic was one line short and the implementation said so rather than adjusting
  the prediction after the fact.

Approval bar:
- Met. `task-003` is complete. `task-007` is unblocked on both dependencies.

Decision: approved
