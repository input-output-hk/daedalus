Implementation: Iteration 1
Timestamp: 2026-09-15T00:26:40Z

Changes made:
- Removed the `/staking/epochs` route binding and the `StakingEpochsPage` import from
  `source/renderer/app/Routes.tsx`, and `ROUTES.STAKING.EPOCHS` from `routes-config.ts`.
- Removed `messages.epochs` and the commented navigation item from
  `components/staking/navigation/StakingNavigation.tsx`.
- Removed the import, the `pageNames` entry and the registration from
  `storybook/stories/staking/Staking.stories.tsx`.
- Removed 11 files: the container, the seven under `components/staking/epochs/`, both dummy JSON
  fixtures and `storybook/stories/staking/_support/Epochs.tsx`.
- Regenerated the four tracked i18n artifacts.
- Recorded four corrections in the `task-004` entry and set its status to `completed`.

Files touched:
- 11 removals
- `source/renderer/app/Routes.tsx`, `routes-config.ts`,
  `components/staking/navigation/StakingNavigation.tsx`,
  `storybook/stories/staking/Staking.stories.tsx`
- `translations/messages.json`, `i18n/locales/defaultMessages.json`, `en-US.json`, `ja-JP.json`
- the four `.agent/plans/storybook-modernization/` files

Verification run:
- `git status --short` before any check: 11 `D`, four source `M`, four artifact `M`, nothing
  untracked outside `.agent/`.
- `components/staking/epochs/` is absent from disk. `storybook/stories/staking/_support/` retains
  its seven other modules, which is the guard that the `_support` directory as a whole was not
  caught up in the removal.
- Greps clean for `StakingEpochs`, `ROUTES.STAKING.EPOCHS`, `staking/epochs`,
  `stakingCurrentEpoch.dummy` and `stakingPreviousEpoch.dummy` across `source/`, `storybook/`,
  `tests/` and `translations/`. `isEpochsInfoAvailable` still appears in `Staking.stories.tsx`,
  which is what the word-boundary grep was designed to leave alone. First acceptance criterion met.
- i18n regenerated by the `task-003` route: 184 lines across four artifacts, every one a deletion
  and not one an insertion. `en-US.json` and `ja-JP.json` each lost exactly the twelve ids, eleven
  `staking.epochs.*` plus `staking.navigation.epochs`.
- `nix build --no-link .#checks.x86_64-linux.i18n` exit 0, which is the check that regenerates and
  compares, so the criterion about `yarn i18n:manage` is answered by the check rather than by a
  claim. `.storybook` exit 0, `.compile` exit 0 on derivation
  `v24rzf5lhqk54xm4s0z9mmh019ixvzzi-daedalus-compile.drv`, `.lint` exit 0 at 5410 warnings against
  5445 after `task-003`. Second acceptance criterion met.
- Sidebar: 258 registrations against 259, with `Decentralization / Staking` going from 14 to 13 and
  nothing else moving. 49 titles and 14 groups both unchanged. Third acceptance criterion met.
- `DelegationCenter.tsx:3` and `:61` still mount `DelegationCenterHeader`, and no file in this
  change set is on that path, so `/staking/delegation-center` still renders the current epoch, the
  next epoch countdown and the sentence about when a delegation change takes effect. Fourth
  acceptance criterion met by the mounting path.

Correction to the approved plan, recorded rather than silently fixed:

- The plan's Non-Goals said "All twelve ids are untranslated in both locales." That is false. All
  twelve carry real translations in both `en-US.json` and `ja-JP.json`, and the Japanese is genuine
  translator output: `ステークプール`, `選出されたスロット`, `現行エポック`, `検索結果はありません`, `前のエポック`,
  `スロット`, `パフォーマンス`, `報酬の割当て`, `エポック`. The assumption was carried over from `task-003`, where
  the six removed ids genuinely were `!!!`-prefixed placeholders in both locales, and it was not
  re-checked for this task's twelve. The outcome does not change, because the screen is unreachable
  and the strings return with a revert, but the plan asserted a materially weaker consequence than
  the real one. The Non-Goals section now states the true position and points here.

One finding deliberately not acted on:

- `source/renderer/app/i18n/locales/babeledit-project.babel` still names the eleven
  `staking.epochs.*` ids after the regeneration, because `yarn i18n:manage` does not write that
  file. It was last committed in 2022, it carries 1950 ids against the 1612 now in `en-US.json`, and
  it does not contain the `staking.chart.tooltip.*` ids `task-003` removed either, because those
  were added after the snapshot. It is inconsistent with the source in both directions already. The
  `i18n` check copies it unchanged and passes. Pruning eleven of its several hundred excess ids
  would be arbitrary, so it is recorded in the task entry and left.

Deviations from the approved plan:
- The one correction above. The removals were performed by the operator against the list this task
  produced, as the plan specifies.

User interaction is now required:
- No.

Outcome: All four acceptance criteria met, the i18n regeneration is pure deletion of the twelve ids,
and four checks are green; ready for code review

Code Review: Iteration 1
Timestamp: 2026-09-15T00:31:15Z

Summary:
- Approved. A route, a container, a component directory, two fixtures and a story registration
  removed together, with the i18n artifacts regenerated in the same commit and the check that tests
  them green.

Blocking findings:
- None.

Non-blocking observations:
- The translation correction is the most important line in this log. "No translation work" and "real
  Japanese translator output is being deleted" are different claims about the same change, and the
  plan made the first one on an assumption inherited from the previous task rather than on a
  measurement. The change is still right, and the record now says why in terms that are true.
  Carrying a finding forward as an assumption is exactly the failure this plan structure is supposed
  to catch, and it was caught by running the regeneration and reading its diff rather than trusting
  the prediction.
- Three stale line references in one entry is the phase's clearest evidence for re-verifying against
  the repository rather than the task graph. Both sources of drift were predicted in the plans of the
  tasks that caused them, so the graph was internally consistent and still wrong about the file.
- Four i18n artifacts against the entry's three is the second instance in two tasks. The pattern is
  that i18n consequences do not appear in acceptance criteria written from a story-and-component
  point of view, and the `i18n` check is the only thing downstream that notices.
- Leaving `babeledit-project.babel` alone is right and the reasoning is the right shape: the file is
  already wrong in both directions, nothing in the pipeline maintains it, and a partial correction
  would make it harder to reason about later, not easier.
- Declining to run prettier over `StakingNavigation.tsx` was correct. The file was already non-clean,
  `prettier:check` is not in the CI check set, and formatting it would have put an unrelated
  five-line reformat of a destructuring into a public diff.
- The `isEpochsInfoAvailable` survivor is the kind of thing that makes a deletion grep untrustworthy
  in either direction. Naming it in the plan before running the grep, and matching on a word
  boundary, is what made the clean result mean something.

Approval bar:
- Met. `task-004` is complete. No task depends on it, and the phase's deletions are finished.

Decision: approved
