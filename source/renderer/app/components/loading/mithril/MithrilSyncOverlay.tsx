// Deleted orchestration component — stub kept for storybook compilation only.
import React from 'react';
export default function MithrilSyncOverlay(_props: Record<string, unknown>) {
  return null;
}
